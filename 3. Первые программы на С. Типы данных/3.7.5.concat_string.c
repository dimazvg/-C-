/* пример на конкатенацию (склейку строк) */
#include <stdio.h>

int main() {
    printf("Hello" ", " "world!" "\n");

    int a = 2, b = 3;
    printf("%d" " + " "%d" " = " "%d" "\n", a, b, a + b);

    return 0;
}